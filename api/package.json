{
  "name": "arcademinter-factory-api",
  "version": "1.1.0",
  "private": true,
  "description": "Authenticated key-inventory API for KMS-encrypted Solana and EVM vanity addresses with concurrency-safe PostgreSQL allocation.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check kms-service.js && node --check security.js && node --check validation.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "aws-sdk": "^2.1478.0"
  }
}
